hazelcast:
  cluster-name: mod-remote-storage-@project.version@
  network:
    join:
      multicast:
        enabled: true
  map:
    default:
      eviction:
        eviction-policy: LFU
        max-size-policy: FREE_HEAP_PERCENTAGE
        size: 40
